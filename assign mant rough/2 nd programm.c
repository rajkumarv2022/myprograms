#include <stdio.h>
 int main()
  {
       int a=60,a1=3,b=55,b1=2,c=48,c1=2;
   int faculty=40,student=20;
    printf("Total Numbers people altogether in a trip:%d",a+b+c+a1+b1+c1);
     printf("\nThe total entry fees for faculty is %d",(a1+b1+c1)*40);
      printf("\nThe total admission fees is %d",(a+b+c)*20+(a1+b1+c1)*40);
        printf("\nThe extra fees required for trip is %d",(a+b+c)*20+(a1+b1+c1)*40-3200);


        return 0;
         }
